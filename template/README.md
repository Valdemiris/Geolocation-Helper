# Geolocation Helper

A simple JavaScript package to assist with geolocation-related tasks such as fetching the user's current location and calculating distances between coordinates.

## Installation

You can install the Geolocation Helper package via npm:

```bash
npm install geolocation-helper
